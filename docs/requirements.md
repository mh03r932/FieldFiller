# FieldFiller — Requirements Catalog

**Status:** Draft v0.1
**Date:** 2026-08-12
**Source:** `docs/vision.md`
**Traces to:** `docs/use_cases.puml`

Roles used in user stories: **Tester**, **Power User**, **Migrating User**, **Auditor**
(see `docs/vision.md` §5).

### Status vocabulary

| | Meaning |
|---|---|
| **Done** | Built, and the cell names the evidence — a harness, a test file, or a CI gate. Four rows are `Done` by **construction** instead: nothing enforces them, they are true because of how the code is written, and each one says so and cites "Guarantees held by construction" below. Those are the only two kinds. A `Done` cell naming neither is a defect in this table, not a third category. |
| **Partial — …** | The behaviour exists but something specific is missing, and the cell says what. |
| **Blocked (X)** | Cannot proceed until X is decided. |
| **Open** | Not started, or measured-and-not-yet-measured for a budget. |
| **Deferred** | Decided against for v1. |

**Reconciled against the test suite on 2026-08-15**, after DD-009. Until that sweep only the
requirements DD-009 touched carried a status at all, so this column read 108 open against a
code base where most of the fill engine was built, verified end-to-end and running in CI. A
column that says nothing is finished when most of it is cannot be used to decide what to do
next, which is the only reason it exists.

A status here is a claim about evidence that exists.

### Guarantees held by construction, with nothing enforcing them

`Done` covers two different situations, and collapsing them would be the kind of quiet
overclaim this column exists to prevent. Most of the requirements below are gated: a CI check
fails the build if they stop being true. These four are not. They are true because of how the
code is written, and they would go on reading `Done` for as long as it took someone to notice.

Tracked here rather than left as prose inside a status cell, so the gap is a list somebody can
work through rather than a sentence somebody has to find.

| | What holds it up today | What would enforce it |
|---|---|---|
| **FR-026** No credential leakage | No generated value is passed to a console, to storage, or to any element but its target. | A source gate rejecting any `console.*` call reachable from the generators, in the shape of `check-network.mjs`. |
| **NFR-010** No retention of page data | Descriptors carry attributes, never values; the one read-back is compared in place and dropped. Its value-versus-identity line is drawn in the requirement itself, not left to reading. | Hard. The nearest practical check is a gate on the descriptor and message types, which would catch a field being added but not a value being smuggled through an existing one. |
| **NFR-030** Descriptor confinement | `FieldDescriptor` has no value-bearing field, and the only thing outliving a fill is one report's field identities, in memory. | A shape assertion over the protocol types, which is the same check NFR-010 wants — plus, now, something to catch a report reaching a storage API. |
| **NFR-031** Generated data lifetime | The operation map is cleared when a fill closes, and nothing writes it to storage. | A test that drives a fill through the background and asserts the map is empty afterwards — the most tractable of the four. |

**Decided 2026-08-15:** offered alongside the NFR-008 and NFR-012 gates, the FR-026 gate was
not taken. That is a deliberate scope choice and not an oversight, which is the reason to write
it down: the next person to read `Done` against FR-026 should find the decision rather than
assume the check exists.

**One residual risk of the same family, added 2026-08-15 with DD-005.** FR-073 reads `Partial`
because the tolerant parser satisfies the requirement's *no-bypass* half — every stored state is
coerced into the current shape and none can be loaded unmigrated — while the migration ladder
is not built. The consequence is not a failing check but a silent one: **a future structural
change to a settings section discards what the parser cannot recognise, and for `rules` that is
the user's own work, with no error shown.** Nothing detects this; the trigger is a human
noticing that a change restructures a section. It is written here, in DD-005, and in the
obligations list of `use_case_catalog.md`, because it is the sort of cost that otherwise gets
paid by a user rather than by a build.

### What the corpus is and is not checked against, added 2026-08-15

`tests/corpus.test.ts` checks the data mechanically: every canton code is a canton, every state
code is a state, every PLZ is four digits inside its canton's range, every ZIP prefix sits in its
state's allocated band, no list holds a duplicate, and one region code never maps to two spellings.
`tests/persona.test.ts` then checks that a generated record keeps those pairings.

**What none of that proves is that a place's real postal code is the one written down.** The
coherence test looks the city up in the same table the generator drew from, so a wrong code typed
into the corpus satisfies it perfectly. The range and band checks narrow the gap to "this is the
wrong real code for this real place" rather than "this is not a code" — the mistakes a person
actually makes when typing a few hundred rows — but the residue is real and no unit test can
close it, because the fact is external to the program.

Stated here because a coherence claim that quietly rests on its own input is the kind of
overclaim this column exists to prevent.

### What the fill report retains, added 2026-08-15 with DD-006

DD-006's options-page report names each field the way the user sees it, which means a control's
**identity** — its label, or failing that its placeholder, name or id — now outlives the fill
that read it. That is page-derived data, so the line NFR-010 and NFR-030 draw is stated here
rather than left to be read either way:

| | |
|---|---|
| **Retained** | One fill's worth of field identities and outcomes, in the background's memory. |
| **For how long** | Until the next fill replaces it, or the background is evicted — which is routine, and why "no report available" is an answer the options page gives in those words. |
| **Never** | Written to storage, in any form, for any duration. Sent anywhere but this extension's own options page, on request. |
| **Not retained at all** | Any *value* from the page. Descriptors have never carried one, and provenance describes how a value was chosen rather than what it was. |

**Two requirements were amended in the same change, because two of them said this.**

NFR-030 previously said descriptors "must not be persisted after the fill completes", which
reads as forbidding this and was written before there was anything to forbid. The requirement's
purpose — nothing page-derived reaches storage, and nothing outlives its usefulness — is
unchanged; what changed is that "persisted" now means written down, and the in-memory window is
bounded and stated.

NFR-010 says the broader thing: never retain "any value originating from a page — existing field
contents, page text, or anything the user typed". A label is page text, so on its letter this
table's first row is a breach — and the amendment adding it was made to NFR-030 alone on
2026-08-15, which left the two requirements disagreeing for as long as it took a review to
notice. NFR-010 now draws the value-versus-identity line in its own text: what a control
*holds* is a value and is never retained; what a control is *called* is its identity, is
retained under NFR-030's stated bound, and is the only page-derived thing that is.

The distinction is worth the amendment rather than a footnote because the two carry different
risk. A field's value is what the user typed and may be a password, a name or a card number. A
field's identity is what the page's own author wrote into the markup, is identical for every
visitor, and is already readable by anything with the page open. Retaining the second is not a
weaker form of retaining the first.

One caveat, kept rather than footnoted away: "identical for every visitor" describes the author's
intent, not a property anything checks. On an authenticated page a label can embed the user's own
text — a greeting, an account number in a legend — and nothing distinguishes those identities from
any other. They stay inside the same bound as the rest (one fill, memory only, never stored),
which is why the tests the "what would enforce it" column names — the shape assertion over the
protocol types, and NFR-031's map-is-empty test — stay on the list rather than being retired as
covered by construction.

Reinterpreting the old wording silently would have been the alternative in both cases, and is
exactly the move this catalog exists to prevent — including when the wording being reinterpreted
is the one nobody thought to check.

---

## 1. Functional Requirements

### 1.1 Fill actions

| ID     | Title                    | User Story                                                                                                                              | Priority | Status |
|--------|--------------------------|-----------------------------------------------------------------------------------------------------------------------------------------|----------|--------|
| FR-001 | Fill All Inputs          | As a Tester, I want to fill every fillable control on the active page in one action so that I can exercise a form without typing.        | High     | Done — a toolbar click fills the reference page across frames and shadow roots; 28 checks in `scripts/e2e-chrome.mjs` |
| FR-002 | Fill Current Form        | As a Tester, I want to fill only the form I am pointing at — resolved by the DD-008 ladder where the page declares no `<form>`, and widened to the page only when I have pointed at nothing — so that I do not disturb other forms on the same page, and never watch a keypress do nothing. | High | Done — DD-008's ladder in full, verified per rung in `scripts/e2e-scopes.mjs`: the owning form, `role="form"`, the smallest block with a submit control, and the refusal that does not widen |
| FR-003 | Fill Selected Input      | As a Tester, I want to fill only the control under my cursor so that I can top up a single field.                                        | High     | Done — one control, or its radio group where the anchor is a member; controls the fill reveals are outside the scope (UC-003 A6). Includes a control inside an open shadow root, which is not free: the pointer and focus are both retargeted to the component, so this scope refused on every such control until 2026-08-16 (BR-003-7, `scripts/e2e-scopes.mjs`) |
| FR-004 | Toolbar Trigger          | As a Tester, I want to fill all inputs by clicking the toolbar button so that the common case takes one click.                           | High     | Done — verified end-to-end, and the trigger every harness uses |
| FR-005 | Keyboard Trigger         | As a Tester, I want at least one fill scope bound to a shipped keyboard shortcut, and every other scope assignable through the browser's own shortcuts settings, so that I can fill without leaving the keyboard and can bind the scopes I use to keys that suit me. | High | Done — `fill-all-inputs` ships bound and all three scopes are assignable and act. A shortcut has no cursor, so the narrower scopes resolve from what is focused, or widen (UC-002 A2). **True since 2026-08-16 and not before:** the sentence was written as an intention, and the agent could not act on it, because the fill instruction did not say which channel sent it and the agent preferred the last right-clicked element whenever one survived. One right-click on a page redirected every shortcut on it thereafter. The instruction now carries a `trigger` (`tests/scope.test.ts`, `tests/protocol.test.ts`). **No harness exercises the command channel itself:** the browser handles a shortcut in its own chrome, so a synthesised key event never reaches the listener, and `scripts/e2e-chrome.mjs` dispatches `action.onClicked` instead. Everything downstream of `startFill` is therefore covered end-to-end by another trigger, and the lines wiring `commands.onCommand` to it are covered by review alone — this line should carry a dated manual result once one is taken. Reviewed 2026-08-17 for the one platform fact that path turns on, after a review claimed the listener's `tab` argument was Firefox-only and every keyboard fill was a silent no-op on Chromium: it is Chromium's own argument — `@wxt-dev/browser` is generated from `@types/chrome`, which declares `(command, tab?)` — and Firefox is the browser that added it late, in 126, under NFR-016's floor of 128. No command is declared `global`, so a shortcut fires only with a window focused and an active tab beneath it |
| FR-006 | Context Menu Trigger     | As a Tester, I want the three fill scopes on the right-click menu so that I can choose a scope from where I am pointing.                 | High     | Done — all three scopes on the menu, each acting on the frame the menu was opened in (`info.frameId`), which is what makes the single-control scope reachable at all (DD-001) |
| FR-007 | Nested Frames            | As a Tester, I want controls inside iframes filled too so that embedded checkout and payment forms are covered.                          | High     | Done — same-origin and cross-origin frames filled from one persona, verified end-to-end |
| FR-008 | Open Shadow Roots        | As a Tester, I want controls inside open shadow roots filled so that web-component design systems are covered.                           | Medium   | Done — open roots filled and closed roots left alone, both verified end-to-end. The *walk* always reached them; the *scopes* did not, and the fixture now carries a component whose control lives in an open root so both halves are held (BR-003-7) |
| FR-009 | Fill Result Report       | As a Tester, I want every fill to return a structured report giving each control exactly one outcome — filled, skipped with a reason, failed with a cause, or unreachable — aggregated per frame and for the operation as a whole, so that I can tell a no-op from a failure and the behaviour is assertable in a test. | High | Done — every frame produces a complete per-control report, the background joins it to what each frame described, and the options page shows every field with its outcome and provenance (DD-006, built 2026-08-15) |
| FR-010 | Error Isolation          | As a Tester, I want a failure on one field not to abort the rest of the run so that one bad rule does not silently halve my form.        | High     | Done — every apply is isolated per control (`fill-loop.ts`); a page handler that throws produces one failed outcome and the run continues (`tests/combobox.test.ts`) |
| FR-076 | Write Verification       | As a Tester, I want every write checked against the control after it is made and again when the fill ends, judged by what the control kind can accept rather than by exact string equality, so that a value the page rejected or reverted is reported as failed instead of counted as filled. | High | Done — both checks landed 2026-08-15, the first at each write and the second in the sweep the loop runs when the page settles |
| FR-077 | Cascade Resolution       | As a Tester, I want fields whose options or availability depend on an earlier answer filled after that answer takes effect — options rewritten, fields revealed, controls enabled — so that a country/state/city form ends complete instead of half-filled from a stale snapshot. | High | Done — the fixpoint loop landed 2026-08-15; verified by the eight cascade rows in `scripts/e2e-cascade.mjs` and by `tests/cascade.test.ts` |
| FR-078 | Honest Cascade Bounds    | As a Tester, I want a fill that cannot settle to stop at a declared limit and tell me it stopped, naming how many fields may be stale, so that a page which fights back produces a bounded honest result rather than a hang or a clean-looking lie. | High | Done — a frame reports its pass count, its cap reason and how many controls a further pass would have covered; the badge marks a capped fill and the tooltip says why, in the user's words (DD-006) |
| FR-079 | User Input Precedence    | As a Tester, I want the extension to stop writing to any control I have touched since the fill began, and to abandon the rest of a cascade the moment I start typing, so that a fill lasting seconds can never overwrite what I am doing. | High | Done — discriminated by `isTrusted` **and** by muting the watcher for the duration of our own write. `isTrusted` alone was not sufficient: a checkbox written with `click()` makes the *browser* fire `input`, so our own tick read as the user typing (found 2026-08-15 by DD-006's badge marker; regression-tested in `tests/cascade.test.ts` and asserted end-to-end) |
| FR-081 | Custom Combobox Filling  | As a Tester, I want ARIA comboboxes and listboxes filled through the keyboard and click interactions a user would use, with the page's state restored and the control reported as skipped when they do not work, so that design-system dropdowns are covered where possible and never left in a broken open state. | Medium | Done — keyboard then pointer, each rung verified through what the control displays; the page's focus, scroll and popup state are restored on every path, including a throw. Bounded per control and per pass (see NFR-036). |

### 1.2 Value generation

| ID     | Title                     | User Story                                                                                                                                | Priority | Status |
|--------|---------------------------|---------------------------------------------------------------------------------------------------------------------------------------------|----------|--------|
| FR-011 | Input Type Coverage       | As a Tester, I want every HTML input type filled with a type-appropriate value so that the page's own validation accepts what I generate.   | High     | Done — every kind in `ControlKind` is recognised and filled; `tests/controls.test.ts` covers each |
| FR-012 | Native Constraints        | As a Tester, I want generated values to respect `min`, `max`, `step`, `minlength`, `maxlength` and `required` so that fields validate.       | High     | Done — `min`, `max`, `step`, `minlength`, `maxlength` and `required` all honoured; verified end-to-end and in `tests/controls.test.ts` (D9) |
| FR-013 | Framework-Safe Write      | As a Tester, I want values applied so that React, Vue and Angular controlled inputs register the change so that filling works on modern apps.| High     | Done — written through the prototype's native setter, which is what defeats React's value tracker (`src/lib/page/apply.ts`) |
| FR-014 | Configurable Events       | As a Power User, I want to turn event dispatch off so that I can fill pages whose handlers misbehave on synthetic events.                    | Medium   | Done — the engine honours it on every control kind, and UC-022 exposes it with the cost of turning it off stated on the screen (BR-022-6) |
| FR-015 | Consent Checkboxes        | As a Tester, I want checkboxes matching my consent keywords always checked so that terms gates never block a test submission.                | High     | Done — consent and required boxes are ticked, verified end-to-end |
| FR-016 | Random Checkboxes         | As a Tester, I want non-consent checkboxes toggled randomly so that I exercise both states over repeated runs.                              | Medium   | Done — a coin flip per box, seeded from the operation |
| FR-017 | Radio Groups              | As a Tester, I want exactly one enabled option chosen per radio group so that the group ends in a valid state.                              | High     | Done — one decision per group, scoped to the owning form; verified end-to-end and by a test that derives the failure rather than asserting the fix |
| FR-018 | Select Elements           | As a Tester, I want one random enabled option chosen for a single select, and for a multi-select a random enabled subset of at least one option chosen without repetition, skipping any option whose value is empty, so that every dropdown ends in a valid submittable state. | High | Done — enabled, non-placeholder options only, every index reachable; verified end-to-end (D3, D7) |
| FR-019 | Generator Types           | As a Power User, I want at least the reference set of generator types (name, email, username, organisation, number, date, telephone, URL, text, alphanumeric, regex, randomized list) so that my existing configuration has an equivalent. | High | Done — thirteen generator types as a discriminated union on `type` (ND-9), tested per type through a real fill |
| FR-020 | Alphanumeric Templates    | As a Power User, I want a template syntax for letters, digits, consonants and vowels so that I can generate format-specific codes.           | Medium   | Done — our own braced template grammar (`{digit:4}`), with unknown placeholders rejected at save time |
| FR-021 | Regex Generator           | As a Power User, I want values generated from a regular expression so that I can match a field's exact accepted format.                      | High     | Done — a documented bounded regex subset; unsupported constructs are refused by name, and generated values are asserted to match their own pattern |
| FR-022 | Randomized List           | As a Power User, I want a value picked from a list I supply so that a field only ever receives values my application accepts.                | High     | Done — randomized list, drawn independently per field and seeded by the control token (FR-080) |
| FR-023 | Coherent Record           | As a Tester, I want one fill to produce a single internally consistent identity — name, username, email, organisation, address and postcode all belonging to the same fictional person — so that the submitted record is realistic rather than a bag of unrelated values. | High | Done — one persona per fill, coherent across every frame; verified end-to-end. **One slot was incoherent from the corpus landing until 2026-08-17:** the second address line was reachable only through `autocomplete="address-line2"`, because the hint list is first-match-wins and `/address|street/` sat above it — so `address2` took the *street's* slot, and the trailing `2` then read as a confirmation marker and wrote the street address onto both lines. A record naming one street twice is exactly what this requirement says it is not. Both halves are now held by `tests/controls.test.ts`: the slot each identity resolves to, and that an ordinal is not a confirmation |
| FR-065 | Sizing by Control Type    | As a Tester, I want generated length to suit the control type, so that a `<textarea>` receives a paragraph rather than the same short phrase a text input gets.  | Medium   | Done — a textarea receives a paragraph where a text input receives a phrase, verified end-to-end; and since 2026-08-17 the per-kind default is configurable (UC-022, BR-022-4), offered only for the free-text kinds where truncation is not destructive |
| FR-024 | Confirmation Mirroring    | As a Tester, I want a confirmation field to receive the same value as the field it confirms, drawn from the same persona attribute rather than replayed from whatever was generated most recently, so that "confirm email" and "retype password" always match their originals regardless of the order the fields appear in. | High | Done — resolved against the persona slot rather than DOM order; both confirmation fields verified end-to-end (D2) |
| FR-080 | Stable Values Within a Fill | As a Tester, I want a control that has to be filled more than once during a single fill — because the page wiped it or replaced it — to receive the same value each time, so that a field and its confirmation cannot drift apart when the page makes us write twice. | High | Done — generation is seeded per control from the operation's seed and the control's token, so a second write is a re-derivation rather than a new draw |
| FR-082 | Persona-Preferred Options | As a Tester, I want a select whose options name places, titles or countries to be given the option matching my generated person where one exists, falling back to a random valid option and saying which happened, so that the address on the form belongs to the same person as the name on it. | Medium | Open — UC-004 generation work, deliberately split from DD-009 so the cascade did not wait on a normalisation table |
| FR-025 | Password Generation       | As a Power User, I want a generated password to satisfy a configurable complexity policy — minimum length, and at least one uppercase, lowercase, digit and symbol by default — so that registration forms accept it. | High | Done — the policy is configurable and reaches generation: length and any subset of the four character classes, satisfied by construction rather than by retrying (BR-019-1), with the example on the screen drawn from the generator a fill uses. Nothing ticked falls back to lowercase and the screen says so (UC-019 A1); where length and composition conflict, length wins (BR-019-2). Verified end-to-end in `scripts/e2e-settings.mjs`, including that a class the policy excludes does not come back when a field's `maxlength` forces the value to be shortened — which it used to |
| FR-072 | Password Field Constraints| As a Tester, I want generated passwords to honour the field's own `pattern`, `minlength` and `maxlength` so that the page's client-side validation passes on the first attempt. | High | Done — `minlength` and `maxlength` are honoured while keeping one character of each class, and `pattern` through a bounded ladder over the character set real forms restrict. An unsatisfiable pattern keeps its value and surfaces as a verified failure rather than a silent bad one |
| FR-026 | No Credential Leakage     | As an Auditor, I want generated passwords never written to the page console or DOM outside the target field so that credentials do not leak into logs. | High | **Done by construction** — no generated value reaches a console, storage, or any element but its target (D5). Nothing *enforces* this the way the network gate enforces NFR-006, and the gate that would was offered and declined: see "Guarantees held by construction" above for what holds it up and what would enforce it |

### 1.3 Field identification and matching

| ID     | Title                     | User Story                                                                                                                                | Priority | Status |
|--------|---------------------------|---------------------------------------------------------------------------------------------------------------------------------------------|----------|--------|
| FR-027 | Matching Sources          | As a Power User, I want rules matched case-insensitively against `name`, `id`, `class`, `placeholder`, associated `<label>` text, `aria-label` and `aria-labelledby` so that I can target fields however they are marked up. | High | Done — `name`, `id`, `class`, label text, `placeholder`, `aria-label` and `aria-labelledby`, each kept separate so a pattern can be anchored and the matched source reported (ND-2). Joined by the test-automation attribute in FR-083, which is a seventh source on the same terms rather than a widening of any of these |
| FR-028 | Source Toggles            | As a Power User, I want to enable or disable each matching source individually so that I can stop a noisy source causing false matches.       | Medium   | Done — the seven toggles bound every rule's effective sources, and UC-018 sets them. The screen states the bound in words and shows how many are enabled, because a rule naming a disabled source matches nothing through it and is otherwise indistinguishable from a rule with a bad pattern |
| FR-029 | Label Text Fidelity       | As a Power User, I want label text taken as rendered text and not as markup so that HTML tag names never take part in matching.               | High     | Done — label text is read as rendered text, never markup (D1 regression test) |
| FR-066 | Implicit Labels           | As a Power User, I want labels that wrap their control recognised as well as those using `for`/`id` so that fields identified only by a wrapping label are still matched. | High | Done — implicit labels through `element.labels`, plus `aria-labelledby` for controls the platform will not label |
| FR-067 | Source-Scoped Rules       | As a Power User, I want to restrict an individual rule to specific matching sources, bounded by the sources I have enabled globally, so that a noisy `class` attribute cannot trigger a rule meant for `name`. | Medium | Done — an optional per-rule source subset, always intersected with FR-028's global toggles. Reachable from the editor as seven checkboxes labelled in words, and a rule scoped to *no* source is refused rather than stored inert (UC-009 A6) |
| FR-068 | Anchorable Patterns       | As a Power User, I want to write anchored and word-bounded patterns that behave predictably so that `^name$` matches only a field actually called "name".      | High     | Done — three match modes (`contains`, `exact`, `regex`); a pattern is never rewritten, so `^name$` no longer matches `username` |
| FR-069 | Match Provenance          | As a Power User, I want the fill report to state which rule matched each field and via which source so that I can debug an unexpected value.                   | Medium   | Done — provenance names the matching rule and the source it matched on, an overridden or skipped rule says so, and the options-page report is the surface that shows it (DD-006). The report had never actually reached that surface until 2026-08-17: the background refused the request from any sender carrying a `tab`, which was meant to exclude content scripts and excluded the options page instead, an options page being a tab. Now covered end-to-end in `e2e-options.mjs`, which fills a page and reads the rows back |
| FR-070 | Rule Validation           | As a Power User, I want an invalid pattern or template rejected when I save the rule, not when a page is filled, so that a bad rule never breaks a fill run.    | High     | Done — `validateRule` rejects unparseable patterns, unknown template placeholders and impossible ranges, and the editor refuses to write a rule that fails it. Since 2026-08-24 the importer applies the same function to every rule it reads out of a file (BR-026-8), which was the one path into storage that bypassed it. With no Save button every keystroke is a save, which is what makes "rejected when you save it" literally true. A rule that still fails at fill time is skipped and named |
| FR-030 | Autocomplete Matching     | As a Power User, I want the `autocomplete` attribute used as a matching source so that standards-compliant forms are recognised without custom rules. | Medium | Done — `autocomplete` purposes are honoured ahead of any identity guess, verified end-to-end |
| FR-083 | Test-Id Matching          | As a Power User, I want rules matched against the field's test-automation attribute — `data-testid` and the five other conventional spellings — so that a component-rendered form whose `id` is generated and whose `name` is absent can still be targeted by a rule. | Medium | Done — a seventh `MatchSource`, resolved by the page agent to one value per control (first spelling present wins) and bounded by FR-028's toggles like every other. Ships **on**, unlike `class`: the attribute exists only where somebody wrote it deliberately, so it is absent rather than noisy on a page that has none. It also names the field in the fill report, ahead of an `id` — `billing-postcode` reads where `:r3:` does not (FR-069) |
| FR-031 | Deterministic Precedence  | As a Power User, I want the first matching rule in my ordered list to win, with profile rules taking precedence over global rules, so that outcomes are predictable. | High | Done — ordered list, first match wins. Profile rules are concatenated ahead of the global list when a profile governs the page (UC-007, built 2026-08-17), so precedence is position and there is no rule-versus-rule comparison anywhere in the engine (BR-007-1) |
| FR-032 | Consistent Match Target   | As a Power User, I want consent, confirmation and exclusion keywords matched against the same identity string as custom rules so that behaviour does not depend on which attribute happens to carry the label. | High | Done — consent, confirmation and exclusion keywords all test the same identity built from the descriptor's sources; rules will join the same function |

### 1.4 Exclusions

| ID     | Title                     | User Story                                                                                                                                | Priority | Status |
|--------|---------------------------|---------------------------------------------------------------------------------------------------------------------------------------------|----------|--------|
| FR-033 | Structural Exclusions     | As a Tester, I want `disabled`, `readonly`, `hidden`, `button`, `submit`, `reset`, `file` and `image` controls never filled so that the page is not corrupted. | High | Done — disabled, read-only, `aria-disabled` and every excluded input type, verified end-to-end |
| FR-034 | Hidden Field Exclusion    | As a Power User, I want to skip visually hidden controls so that off-screen and collapsed fields are left alone.                             | Medium   | Done — every hiding technique in UC-005 A3 has its own test, plus the control case of a plainly visible field |
| FR-035 | Pre-filled Exclusion      | As a Power User, I want to skip controls that already hold a value so that I can top up a partially completed form.                          | Medium   | Done — the engine honours it, our own earlier writes correctly not counting as content, and UC-020 exposes it beside the exclusion list |
| FR-075 | Refill With a New Persona | As a Tester, I want to fill the same page again and receive a completely new person, even when I have chosen to skip pre-filled fields, so that I can submit a form repeatedly with fresh data. | High | Done — a control this extension wrote is never treated as user content, so the same page fills twice (`tests/controls.test.ts`) |
| FR-036 | Ignored Field Patterns    | As a Power User, I want a list of regular expressions identifying fields to never fill so that CAPTCHAs and honeypots are left untouched.     | High     | Done — patterns are compiled once per fill and applied to every source, and UC-020 authors them in all three match modes. An unusable pattern is stored and reported beside the field rather than refused, because the agent skips it with the rest of the list intact (UC-005 A5) and refusing would discard a half-typed pattern on every keystroke. One carve-out, corrected here 2026-08-17 after the text and the parser were found to disagree: an *empty* pattern is reported and does **not** survive, because it is not unusable — `contains` of `''` is contained in every field name and an empty regular expression matches every one, so the blank row a click on "add" creates would exclude the whole page rather than nothing. *Restated 2026-08-18*, because "not stored" named the wrong end: it is the **reader** that drops it, not the editor that refuses it. `parseMatcher` returns nothing for a blank pattern, so the row stays on screen and is written by the page that created it, and is gone by the next load — the same moment, in the same file, at which `globs` drops a blank domain or profile address. The citation moved with the claim: `src/lib/settings.ts` and `tests/settings.test.ts`, where the blank pattern is now named in the list a parse is expected to drop. `tests/exclusions.test.ts` was the wrong witness — it covers what the editor *says* about a blank pattern, never what survives a reload (UC-020 A1) |
| FR-071 | Honeypot Avoidance        | As a Tester, I want a field treated as hidden when the browser reports it as not visible, or it has zero rendered area, or zero opacity, or is clipped away, or is positioned outside the document bounds, or is marked `aria-hidden` with a negative tab index, so that anti-bot honeypots are never filled. | High | Done — the perceivability set is enumerated rather than inferred, and each member is asserted against a laid-out control so the reason under test is the one that decides |
| FR-037 | Excluded Domains          | As a Power User, I want a list of patterns identifying domains where filling is disabled so that the extension is inert on my production and banking sites. | High | Partial — patterns are matched and every scope is refused on a match (`tests/scope.test.ts`, `scripts/e2e-scopes.mjs`), including on a URL carrying a port, which until 2026-08-16 no pattern matched at all: the exclusion was accepted, listed and silently inert, failing open (BR-008-6). The end-to-end harness cannot exercise the *pattern* path: reading a tab's URL needs `activeTab`, which follows a real user gesture and cannot be synthesised, so the harness covers UC-008 A1 (unreadable address) instead — and that gap is why the port defect survived to a review rather than to a red build. The screen that edits the list is built (UC-021, 2026-08-17): globs in the vocabulary of extension match patterns, validated for the two faults a glob can have — empty, and containing whitespace, which matches nothing and so fails *open*. A bare `*` is accepted deliberately, since there is no other global off switch, and UC-021 A3 records what per-keystroke storage costs: a pattern passes through `*` on its way to `*.example.com`, and a fill during those keystrokes is refused. That direction is closed and self-correcting. The end-to-end gap is unchanged and structural |
| FR-074 | Structural Domain Exclusion | As a Power User, I want an excluded domain to mean the extension does nothing there at all — no fill, no message to the page, no control read — so that listing a domain is a guarantee and not a preference. *(Rewritten 2026-08-15: this asked for non-injection until BR-008-4 declined it. The agent is still injected; it is never asked to act.)* | High | Done — against the requirement as rewritten on 2026-08-15: no fill runs, no message reaches a frame and no control is read on an excluded domain (`scripts/e2e-scopes.mjs`). The original asked that the code never be *injected* there; that needs runtime content-script registration in place of the manifest's static declaration, and was considered and declined (BR-008-4). **Our code still loads on an excluded page and does nothing** — the weaker guarantee is stated rather than claimed away |
| FR-038 | Exclusion Indicator       | As a Tester, I want a visible indicator when the current domain is excluded so that I understand why nothing happened.                        | Medium   | Done — a standing badge and a tooltip naming the pattern, cleared on navigation without reading where the tab went (BR-008-3) |
### 1.5 Rules and profiles

| ID     | Title                     | User Story                                                                                                                                | Priority | Status |
|--------|---------------------------|---------------------------------------------------------------------------------------------------------------------------------------------|----------|--------|
| FR-039 | Create Rule               | As a Power User, I want to create a custom field rule with a generator type and match patterns so that a field gets data my application accepts. | High  | Done — a rule is created from the options page with defaults that are valid on arrival, appended so existing rules keep their precedence (`scripts/e2e-options.mjs`) |
| FR-040 | Edit Rule                 | As a Power User, I want to edit an existing rule so that I can correct it as the application changes.                                        | High     | Done — edited in place, keeping identity and position; a change of generator type keeps the name, matcher, scoping and persona flag and discards only the options that mean nothing to the new type |
| FR-041 | Delete Rule               | As a Power User, I want to delete a rule so that my list stays relevant.                                                                     | High     | Done — deleted immediately with an undo that restores the rule to the position it held, rather than a confirmation before every deletion |
| FR-042 | Reorder Rules             | As a Power User, I want to reorder rules so that I control which one wins when several match the same field.                                 | High     | Done — move up and move down, keyboard-operable by construction rather than as an accommodation, with the new position announced (BR-012-1, NFR-019) |
| FR-043 | Preview Rule Output       | As a Power User, I want to see sample values a rule generates before saving so that I can verify a template or regex without reloading a page.| Medium   | Done — several samples from the real generators, regenerated as the rule is edited, cleared rather than left stale when the rule stops validating (UC-013) |
| FR-044 | Rule List Ceilings        | As a Power User, I want the extension to impose no rule cap of its own, and to tell me where the platform's own ceilings are before I reach one, so that a large application is covered and a limit is something I read rather than discover.                | High     | **Done — rewritten 2026-08-28, because the title was a promise the platform does not let anyone keep.** The product imposes no cap: nothing counts rules, and the list is an array in one stored state. What exists instead is three platform ceilings, each measured rather than quoted, and the requirement is now that each is named where the user meets it. **Local storage — 44,840 global rules** beside the fixture's four profiles, at 234 bytes a rule, stopping on `kQuotaBytes` at 10,476,739 of the 10,485,760 the browser reports (`pnpm run spike:syncquota`, Chromium, 2026-08-28). Not pre-announced anywhere, and deliberately: it is two orders of magnitude past the largest configuration this project has measured, and the write that would cross it is refused whole and reported by name (NFR-021, UC-024 A2). **Synchronisation — 399 rules**, the ceiling DD-002 measured and UC-029 A1 is built around. This is the one a user can actually reach, so it is the one said in advance: BR-029-3 puts it on the sync screen as standing text from 2026-08-28, present before the feature is ever switched on, and `scripts/e2e-sync.mjs` asserts it is there with the toggle still off. **The options editor — measured to 500 rules** with roughly 4× headroom against NFR-024's 100 ms, and O(rules) per interaction beyond that. Unbounded but not free, and the number is NFR-024's rather than a second claim here. None of the three is a cap this project chose, and saying so is the whole of the rewrite: the old wording put the extension behind a guarantee only the smallest of the three would have honoured. |
| FR-045 | Create Profile            | As a Power User, I want to define a named rule set scoped to URLs matching a pattern so that each application I test gets its own data.       | High     | Done — UC-014. A named rule set with glob address patterns, appended so an existing profile keeps governing what it governed, and reorderable because order is the only tiebreak (BR-014-2). A profile with no patterns matches *nothing* rather than everything, and is marked as such while it is being written: the reading whose failure is visible (BR-014-1) |
| FR-046 | Manage Profiles           | As a Power User, I want to edit and delete profiles so that they track the applications I actually work on.                                  | High     | Done — UC-015 and UC-016. Edit through the same rule editor the global list uses, pointed at the profile's rules by a lens — one editor, so a profile rule cannot become validatable differently from a global one (BR-014-5). Deleting is confirmed and names the rule count, which is the one place this list departs from the rule editor beside it and BR-016-1 says why |
| FR-047 | Active Profile Indicator  | As a Tester, I want to see which profile applies to the current page so that I can tell whether my scoped rules are in effect.                | Medium   | Done — UC-017, as a fill *result* rather than a page indicator, and the difference is forced rather than chosen (BR-017-3). The extension learns a page's address only when a fill is invoked — that is what `activeTab` grants and what keeps `tabs` off the manifest — so an indicator showing the active profile *before* a fill would require watching the user browse, which NFR-008 forbids and whose absence is a stated guarantee. The report names the profile that applied — by label, or by its first address pattern where the user has not named it yet, so an unnamed profile is never mistaken for no profile (BR-017-5) — and says so in words when none did (BR-017-4). No end-to-end cover for the matching path: `activeTab` cannot be synthesised, the same structural gap FR-037 records |

### 1.6 Settings lifecycle

| ID     | Title                     | User Story                                                                                                                                | Priority | Status |
|--------|---------------------------|---------------------------------------------------------------------------------------------------------------------------------------------|----------|--------|
| FR-048 | Default Configuration     | As a Tester, I want a useful default rule set on first install so that the extension is valuable before I configure anything.                | High     | Done — the useful default is the engine, not a rule set: the list ships empty by design (DD-005) and an unmatched field falls through to the persona-driven generator, so a user who writes no rules gets every control kind, native constraints, coherent records and dependent fields (BR-028-4). The honest form is narrower than the written one, after FR-047 and FR-074, and UC-028's restore lands that same state behind a quantified confirmation |
| FR-049 | Behaviour Defaults        | As a Power User, I want to configure default maximum length, consent keywords and confirmation keywords so that generic filling suits my forms.| Medium  | Done — UC-022. Default maximum length per free-text control kind (FR-065, and only where the control declares none of its own), plus consent and confirmation keyword lists, which this requirement is what added to the schema. Two rules deliberately stay out of the user's hands and the screen says which: a required checkbox is always ticked, and a trailing ordinal always marks a confirmation (BR-022-3) |
| FR-050 | Trigger Configuration     | As a Power User, I want to enable or disable the context menu and to reach the browser's shortcut settings so that I control how fills are invoked. | Medium | Done — UC-023. The context menu is the one configurable trigger; the toolbar carries no control (BR-023-2) and the keyboard cannot be bound by any extension, so the screen offers the browser's own address and says that is what it is (BR-023-1). Entries are rebuilt on every settings change and on every background start, so the menu and the setting agree at the next right-click with no reload — asserted end-to-end in `scripts/e2e-settings.mjs` by asking the browser whether the entry exists |
| FR-051 | Live Propagation          | As a Power User, I want settings changes to take effect in open tabs without reloading them so that iterating on a rule is fast.             | High     | Done — and by construction rather than by propagation: settings travel with each fill instruction, so no agent holds a copy that could go stale and there is nothing in a tab to update (BR-024-6). Verified end-to-end — a rule authored in the options page changes what the next fill writes |
| FR-052 | Export Settings           | As a Power User, I want my configuration exported as plain, pretty-printed JSON so that I can diff it, review it and keep it in version control alongside my team's other config. | High | Done |
| FR-053 | Import Settings           | As a Power User, I want to import a configuration file so that I can restore a backup or adopt a colleague's setup.                          | High     | Done — `lib/settings-import.ts` plans the import, the options page shows the plan and writes it as one replacement (UC-026, BR-026-1, BR-026-6). `scripts/e2e-import.mjs` exports a configuration through UC-025 and imports it back, asserting storage ends where it started |
| FR-054 | Import Validation         | As a Power User, I want an import that is malformed or from an unsupported version rejected with a clear reason so that I do not silently lose my configuration. | High | Done — four refusals, each naming its own cause and offering no way past it (UC-026 A1, A2, A5; BR-026-2). The one that carries this requirement is A5: the tolerant parser cannot fail, so without it a file from another tool imports as a full set of defaults and reports success. Every refusal is exercised in `scripts/e2e-import.mjs`, storage asserted unmoved |
| FR-073 | Schema Migration          | As a Power User, I want an older configuration migrated to the current schema automatically, with no option to load it unmigrated, so that a bypass can never write inconsistent data into my settings. | High | Partial — the tolerant parser coerces any stored state into the current shape with no bypass (ND-13), and lifts the pre-DD-005 flat shape. DD-005 accepted, explicitly, that a future *structural* change loses what it cannot recognise; a sequential ladder is the named remedy and is not built. **Its absence is now *reported* rather than only accepted (2026-08-22, UC-026):** an import names every entry and every key it could not keep, before the write, so what the parser drops in place of a migration step is visible to the user instead of silent (BR-026-3, BR-026-7). That is not the ladder and does not close this row — it is what makes the gap survivable until the ladder exists. Tested against on 2026-08-17: Phase 4 added a `triggers` section and two keyword lists inside `behaviour`, all additive keys that default when absent, so none of them is the restructuring that would force the ladder — see the catalog's note under "Obligations Carried by Unwritten Specs" |
| FR-055 | Migrate From Fake Filler  | As a Migrating User, I want to import a Fake Filler backup file so that I can switch without rebuilding my rules by hand.                    | High     | Done — built 2026-08-26 with UC-027: `lib/fakefiller-migrate.ts` translates the reference's documented schema (BR-027-2) — plain JSON or its Base64 transport — section by section into ours, as a plan nothing writes until confirmed. Every pattern list joins into one `regex`-mode rule through `validateRule` (BR-027-3); nothing is guessed into an active state (BR-027-5). Verified end-to-end by `pnpm run migrate:chrome` |
| FR-056 | Migration Report          | As a Migrating User, I want a summary of what was imported and what could not be mapped so that I know exactly what to fix up.               | Medium   | Done — the report is UC-026's two-list shape with migration's own sentences: dropped (gone) and noted (arriving changed) are separate lists on screen, each entry a line about the thing the user recognises rather than a count (A3, FR-056's heart). The version preamble leads when the backup states a version other than the documented one (A2), and BR-027-6's persona sentence is said once above both lists |
| FR-057 | Restore Defaults          | As a Power User, I want to reset all settings to defaults with a confirmation step so that I can recover from a broken configuration.        | Medium   | Done — built 2026-08-24 with UC-028: the confirmation quantifies the loss (rules, profiles, both exclusion lists) and names export as the way back before the write (BR-028-2, BR-028-5); the write itself is the import's single-replacement path, so a rejected write leaves the previous configuration whole. Verified end-to-end by `pnpm run restore:chrome` |
| FR-058 | Cross-Device Sync         | As a Power User, I want my configuration synchronised across my signed-in browsers with no account of my own so that I get the same setup everywhere. | High | **Done — UC-029, built 2026-08-28.** The browser's own synchronised storage, opted into per device and off by default (BR-029-1), with no account of ours anywhere in it. `lib/sync.ts` is the layout DD-002 measured — rules and profiles sharded eight to a key, every other section a key of its own — and `platform/sync-store.ts` executes what it plans. Verified end-to-end by `pnpm run sync:chrome`, which drives a real `chrome.storage.sync`. **Firefox carries the identical engine** — one source tree, no per-target branch (NFR-013), and the built background bundles are byte-for-byte the same calls. Its platform semantics were measured in a real Gecko on 2026-08-28 rather than assumed: writes accepted with no account signed in, both quotas enforced, `storage.onChanged` firing for the `sync` area, and the refusal wording `isQuotaFailure` classifies. Two differences were found and neither needed a change, because the design had already refused to depend on either — Firefox exposes none of the `QUOTA_BYTES*` constants (the ceiling is found by attempting the write), and it returns stored objects in insertion order where Chromium alphabetises them (the delta compares through a key-sorting stringify). What is *not* covered there is this extension's own wiring: WebDriver BiDi refuses to navigate to a `moz-extension:` URL, so no harness can reach the add-on's options page or storage — the same structural gap NFR-014 already records, not a new one. **One limitation is accepted rather than fixed, decided 2026-08-28.** A device pushes a delta computed against the store as it is *now*, not against the state it last agreed with, so an edit made on a stale baseline can write back differences it never made — reverting another device's concurrent change with a reach larger than the eight rules BR-029-3 names. The frequent instance is closed (`hasUnsentChange` stops a device echoing back a configuration it has just adopted); what remains needs two devices editing within one read-to-write gap of each other. Closing it needs a generation per shard and a compare-and-set, which is a change to the *stored layout* — and that is the reason it is not being done: today's build reads a shard as a bare array and would silently see **zero rules** in a shard carrying a generation, so shipping it without FR-073's migration ladder would empty rule lists on every device that had not updated yet. See the Deferred table in `implementation_plan.md` for the trigger to revisit. **The honest form is narrower than the written one, and this is the fourth requirement to need saying so** after FR-047, FR-074 and FR-048: "consistent everywhere" is a state of the user's browser account that no extension API reports (A4). What is built and what the screen claims is written, refused, or stopped — never that another device is up to date. The harness asserts the disclaimer as firmly as it asserts the write. |
| FR-059 | Sync Failure Visibility   | As a Power User, I want to be told when sync cannot store my configuration so that I do not believe a device is up to date when it is not.    | Medium   | **Done — UC-029 A1, built 2026-08-28.** Past the store's capacity the write is refused, nothing is truncated (BR-029-5), local storage keeps every rule, and the replica's index is marked not current so no *other* device applies a configuration that stopped being carried. The screen says three things in one sentence: that it stopped, what stopped it in rules against the ceiling, and what restores it — remove rules, or turn synchronisation off and keep the configuration local. Exercised in `scripts/e2e-sync.mjs` at 900 rules against a real quota, with the local rule count asserted intact on the other side. Two supporting behaviours were found by that harness rather than reasoned into existence: a queued retry no longer paints "a change is queued" over a stop the user has to act on, and the queued sentence no longer promises when the write will go — the timer lives in a worker the browser may stop at any time. |

### 1.7 Transparency and support surfaces

| ID     | Title                     | User Story                                                                                                                                | Priority | Status |
|--------|---------------------------|---------------------------------------------------------------------------------------------------------------------------------------------|----------|--------|
| FR-060 | Shortcut Discovery        | As a Tester, I want to see my current shortcuts and reach the browser page that changes them so that I can set up my keys.                   | Medium   | Open   |
| FR-061 | Changelog                 | As a Tester, I want to read what changed in each version so that I understand new behaviour after an update.                                 | Low      | Open   |
| FR-062 | Attribution               | As an Auditor, I want the extension to show its licence and the upstream MIT attribution so that provenance is clear.                        | High     | Open   |
| FR-063 | Build Verification        | As an Auditor, I want each release to publish its source tag and artefact digest so that I can confirm the store build matches public source.| High     | Open   |
| FR-064 | Undo Last Fill            | As a Tester, I want to restore the values a fill overwrote so that I can recover from filling the wrong form.                                | Low      | Deferred |

---

## 2. Non-Functional Requirements

| ID      | Title                      | Requirement                                                                                                                    | Category        | Priority | Status |
|---------|----------------------------|---------------------------------------------------------------------------------------------------------------------------------|-----------------|----------|--------|
| NFR-001 | Fill Latency               | A page with 500 fillable controls must be completely filled within 500 ms measured from trigger to the last event dispatched **in the first pass**. Scoped to the first pass by DD-009: this is the budget for what the user perceives as the form filling, and it is unaffected by any cascade the page then runs, which is bounded separately by NFR-034. | Performance | High | **Done — measured 2026-08-28** (`pnpm run spike:filllatency`, Chromium, 500 controls across ten kinds, nine fills). **97 ms median and 102 ms worst on a warm background**, against a 500 ms budget; 111 ms on the cold fill where the trigger itself starts the worker, which NFR-027..029 bound separately. Roughly five times the headroom the requirement asks for. **What is timed is each control's *first* write, and the last of those**, which is how the first pass is separated from the settle loop without trying to observe a pass boundary from outside: every control is written once in the first pass, so the maximum of the firsts is the moment that pass finished, and it is immune to the ~1,150 events the whole operation dispatches. Coverage is accounted for exactly on every run — 500 controls = written + the checkboxes a coin flip left unticked, which need no write and dispatch nothing. Two harness faults were found and are recorded in the spike rather than worked around: waiting for writes to go quiet instead of for the badge left the tab in `filling` and had two thirds of the runs silently measuring nothing, and counting an unticked box as an unfilled control understated coverage by about half the boxes on the page. |
| NFR-002 | Trigger Responsiveness (warm) | With the background context already running, the first field must receive a value within 100 ms of the trigger on a page with fewer than 50 controls. | Performance | Medium | Done — 3.0 ms measured against a 100 ms budget, with the corpus in place (`scripts/spike-coldstart.mjs`, 2026-08-15) |
| NFR-003 | Page Agent Size            | The page agent bundle must not exceed 40 KB **minified and uncompressed**. Compressed size is not the metric — an extension bundle is read from disk, never transferred, so parse cost scales with uncompressed bytes. **The derivation this budget used to cite was measured on 2026-08-28 and did not survive** (`pnpm run spike:pagecost`). It read: *"derived from NFR-005: at ~0.1 ms/KB for parse and compile, 40 KB costs ~4 ms per frame, staying inside the 15 ms page-load budget across up to three frames"*. Parse and compile actually cost **0.014 ms/KB** — seven times less — so on that argument alone the budget could be several hundred kilobytes. The agent's real cost to a page is +2.2 ms on one frame (NFR-005), and only ~0.3 ms of that is parsing its own bytes; the rest is the fixed cost of injecting a content script at all, which does not grow with the bundle. **So 40 KB is not load-derived and the number stands on its own footing: G6, a page agent an order of magnitude smaller than the reference's, which is one of the three differentiators DD-001 left this project after it gave up the minimal-permissions claim.** The gate is unchanged and stays in CI; what changed is which argument it answers to, and a budget defended by a figure that is wrong by 7× is a budget nobody can reason about. | Performance | High | Done — enforced in CI on every build for both targets; 18.5 KB of 40 KB today |
| NFR-027 | Trigger Responsiveness (cold) | After background-context eviction, the first field must receive a value within 400 ms of the trigger, covering service-worker restart plus corpus load. | Performance | Medium | Done — 14.0 ms measured against 400 ms, now **with** the real corpus bundled (`scripts/spike-coldstart.mjs`, 2026-08-15). No longer a floor: the figure the budget was reserved for is included in it |
| NFR-028 | Corpus Load Budget         | Loading the data corpus in the background must complete within 250 ms on a cold start, and must happen once per background lifetime rather than per fill. | Performance | Medium | Done — bounded rather than isolated, and the bound is the stronger claim. The corpus is a statically bundled module (a `fetch()` of an extension URL would fail the network gate), so it is parsed inside the worker's own start-up and has no separate load to time. That whole restart is **11.0 ms against a 250 ms budget**, which is an upper bound on the corpus's share of it |
| NFR-029 | Fill Round-Trip Overhead   | Each page-agent-to-background round trip must add no more than 20 ms to a warm fill, independent of how many fields are in the descriptor batch, and a frame must make no more round trips per fill than DD-009's pass cap. Bounding the count rather than rewording the budget keeps the total derivable instead of asserted; frames run concurrently, so the serial cost within one frame is the figure that binds. | Performance | Medium | Done — 0.2 ms per round trip against a 20 ms budget, and DD-009's pass cap bounds the count per frame |
| NFR-034 | Cascade Resolution Budget  | After the first pass, a fill must reach a settled state or declare itself capped within 5 s of wall-clock time and a fixed pass cap, whichever comes first, with the per-pass wait for the page to go quiet bounded independently. The caps must be chosen from measured cascade depth across the DD-009 fixture matrix, not assumed, and a fill that hits one must report it (FR-078). | Performance | High | Done — 5 s budget, 8 passes, 1.5 s per-pass wait, 3 writes per control. The pass cap was measured on 2026-08-15 by lowering it until the matrix broke: it fills in three passes and settles in four, and the shipped cap is that doubled. |
| NFR-036 | Combobox Interaction Budget | Driving custom comboboxes must consume no more than 250 ms per control and 2 s per pass, and a control the budget does not reach must be reported as skipped rather than silently omitted or waited for. Unlike a write, driving a combobox means interacting and waiting for the page to render, once per rung — so a page built entirely from design-system selects is the shape that threatens NFR-001, and the selector that finds them is not. | Performance | High | Done — `Bounds.comboboxControlMs` caps one control and `Bounds.comboboxPassMs` caps the pass, both in milliseconds of measured time (`src/lib/page/fill-loop.ts`), asserted in `tests/cascade.test.ts` from both directions: fast controls are not cut off while budget remains, and slow ones still stop at the bound. Corrected 2026-08-15 — the pass budget was previously charged the per-control *cap* per attempt, making it an attempt counter that exhausted after eight tries however little time they took. One gap remains: a control skipped because the budget ran out and one that could not be driven report the same reason, so the report cannot distinguish them |
| NFR-035 | Cascade Observer Teardown  | Any mutation observer, element-token map or input listener a fill installs must be released when its report is sent, and none may outlive the operation. The page agent is present on every page the user visits (DD-001), so anything left running is paid for on every page rather than on the filled one. | Performance | High | Done — released in a `finally`, and asserted by counting listener and observer connects against disconnects in `tests/cascade.test.ts` |
| NFR-004 | Idle Footprint             | The page agent must add no more than 2 MB of heap per tab while idle.                                                           | Performance     | Medium   | **Done — measured 2026-08-28** (`pnpm run spike:pagecost`, Chromium). **+248 KB of JS heap** against a 2 MB budget, eight times the headroom. Measured as the difference between two browsers loading the same page — one with the extension, one with no extension at all — because an absolute heap figure is a statement about the machine rather than about the agent. Garbage is collected before every read and five samples are taken per browser, since an uncollected heap drifts between reads and the drift would be attributed to whichever browser was sampled later. A content script shares the renderer's isolate, so its heap is counted in the page's figure — which is what makes the difference the right number. |
| NFR-005 | Page Load Impact           | Extension presence must add no more than 15 ms to page load, measured as median over 20 loads of a reference page.              | Performance     | Medium   | **Done — measured 2026-08-28** (`pnpm run spike:pagecost`, Chromium, 20 measured loads a browser after 3 discarded, served `no-store`). **+2.2 ms** against a 15 ms budget — 22.4 ms median with the extension against 20.2 ms without. This is the cost DD-001's persistent injection has to keep paying on every page the user never fills, so it is measured over *presence* rather than over work. |
| NFR-006 | Zero Autonomous Network    | The extension must issue no outbound network request the user did not initiate: no telemetry, no update checks, no remote configuration, and no remotely hosted font, stylesheet, script or image. A navigation the user starts by activating a link is not a request by the extension and is out of scope here. Verification is specified by NFR-033. | Security | High | Done — enforced in CI by `scripts/check-network.mjs` across every shipped file in both targets |
| NFR-033 | Network Absence Verification | Absence of network capability must be established by static analysis of the built bundle in CI — no `fetch`, `XMLHttpRequest`, `WebSocket`, `EventSource` or `sendBeacon` reachable in shipped code, and no external URL in any shipped HTML, CSS, or the manifest. Runtime traffic monitoring in Chromium runs in addition, never instead: it can only demonstrate the code paths it exercises, and it cannot observe requests originating in the background context without browser-level instrumentation. | Security | High | Done — that gate is the verification NFR-006 asks for, and it runs on every build |
| NFR-007 | No Remote Code             | All executable code must ship inside the package; no `eval`, no `new Function`, no injected remote scripts.                     | Security        | High     | Done — same gate; no remote code capability in either target |
| NFR-008 | Declared Permissions       | The extension must request no permission beyond `storage`, `contextMenus`, `scripting`, `activeTab` and a content script matching all URLs (per DD-001), and must not request `tabs`, `webRequest`, `cookies`, `history` or `downloads`. Marketing and store copy must not describe the extension as minimal-permission. | Security | High | Done — `scripts/check-permissions.mjs` fails the build on any permission outside the five, on any host permission, and on any permission it has never heard of |
| NFR-009 | Pattern Vulnerability Rejection | A user-supplied pattern must be rejected when it is stored if static analysis identifies it as vulnerable to catastrophic backtracking. Rejection at authoring time is the primary control, because a running evaluation cannot be interrupted once started. "Authoring" includes importing: a file is authored somewhere else, and BR-026-8 puts it through the same check. | Security | High | Partial — `analysePattern` rejects the three catastrophic shapes (nested quantifier, nullable repetition, overlapping alternation) at save time, structurally rather than by timing. DD-005 allows a *match* pattern to use wider syntax than the analyser models, so it is a filter for the known shapes and **not a proof of safety** — stated here rather than implied. Domain-exclusion globs are a separate surface and are no longer a regular expression at all: they compile to nothing and match by a forward scan, which is a bound rather than a filter (BR-008-7). Until 2026-08-16 they were translated `*` → `.*`, giving the background a catastrophic-backtracking path that ran before every fill — 33 s on a 71-character URL, measured |
| NFR-032 | Pattern Evaluation Containment | No pattern may be applied to page-supplied text unless it has passed `validateMatcher` — at authoring, at import, and at the boundary into the page — and a fill whose pattern matching exceeds a declared bound must name the responsible patterns in its report. **Containment is at the boundary, not at evaluation:** a running regular expression cannot be interrupted, so the bound attributes an overrun rather than preventing one. | Security | High | **Done — rewritten and then built 2026-08-28, because the mechanism the old text prescribed was measured and neither half of it works.** It read: *"any input a stored pattern is applied to must be truncated to 1,024 characters, and an evaluation exceeding 250 ms must be recorded with the affected field reported as failed"*. **Truncation bounds nothing.** The patterns that hang a tab do it at **40 to 86 characters** — `^(\s[\w-]+)+$` against ordinary utility-class strings costs 287 ms at 40, 18.9 s at 50 and 55.7 s at 86 — an order of magnitude under the 1,024 the requirement names, so the bound sits far above the cliff. And the *linear* cost it might have bounded instead is not a threat either, measured 2026-08-28 in V8: a safe pattern over a **10 MB** identity costs 0.4 ms, and `contains` 0.97 ms. There is no input length at which truncation to 1,024 characters saves anything. **A 250 ms budget cannot be enforced**, which the old text conceded in its own last sentence and then asked for anyway: JavaScript has no way to interrupt a running regular expression, so nothing can act on the budget while it is being exceeded. **What actually contains the threat is refusal at the boundary**, and that half is already true: `analysePattern` refuses the catastrophic shapes in the rule editor (NFR-009), BR-026-8 applies the same check to every rule in an imported file, and since 2026-08-24 a field exclusion whose pattern `validateMatcher` refuses is no longer sent to the page at all — an exclusion being the one surface stored faulty on purpose (UC-005 A5, UC-026 A8). **Built 2026-08-28.** Matching is timed at both sites and charged to the *pattern*, and the report names what overran: `slowRules` for the rule matcher, which runs in the background, and `slowExclusions` for field exclusions, which run in the page agent against every control before any rule does — the sharper of the two, because a hang there is the user's tab rather than a worker. Both accumulate across every frame and every pass, because the bound is stated over a fill. `MATCH_RULE_MS` is **100 ms**: 500 controls against an ordinary rule is well under a millisecond in total, and the pattern this exists for costs 287 ms against *one*, so the bound sits far above the first and far below the second. The clock is injected at both sites — `BatchSource.now` beside `randomFor`, and the fill loop's existing `scheduler.now` — which is what lets `tests/rules.test.ts` and `tests/cascade.test.ts` assert the accounting against a clock the test controls rather than against a machine that has to genuinely be slow. **Corrected while building it:** the first draft of this rewrite inherited "the affected field reported as failed" from the old text, and that is untrue twice over. Matching slowly does not stop a control being filled, so reporting it failed would assert the opposite of what happened. And a catastrophic pattern is slow against *every* control, so a per-field line would repeat itself five hundred times over one deletable rule — slow matching is a property of the **configuration**, not of a field. Both sentences say so: nothing was lost, the pattern is what is slow. **Accepted residual:** a pattern the analyser admits that is still catastrophic. NFR-009 is a filter and not a proof, and says so. In the 55 s case the operation timeout is what ends the fill; this is what turns the 287 ms case from an unexplained slow fill into a named pattern the user can delete. |
| NFR-010 | No Retention of Page Data  | The extension must never retain any **value** originating from a page — existing field contents, page text, or anything the user typed — beyond the operation that read it, and must never write such a value to storage or include it in a message beyond the fill it belongs to. A control's **identity** — the label, placeholder, name or id by which a user recognises it — is page-derived but is not a value under this requirement; NFR-030 states the only window in which it may be held and the bound on it. | Security | High | **Done by construction** — no page *value* enters a descriptor, a message or storage; the one read-back is compared in place and dropped (BR-034-11). Nothing gates this; see "Guarantees held by construction" above for why a gate is hard here. Amended 2026-08-15 by DD-006, in the same change as NFR-030: field *identities* now outlive the fill, in background memory only, for one report — see "What the fill report retains" above |
| NFR-031 | Generated Data Lifetime    | Synthetic data the extension generates must be discarded when a fill completes, unless the user has explicitly asked for it to persist across fills; where they have, it must be held in volatile memory only, be discardable by a single user action, and never be written to storage or transmitted. | Security | High | **Done by construction** — the persona and every generated value are dropped when the operation closes. Nothing gates this, and of the four it is the one most easily gated: a test driving a fill through the background and asserting the operation map is empty afterwards. See "Guarantees held by construction" above |
| NFR-030 | Descriptor Confinement     | Field descriptors sent from the page agent to the background must carry only matching-relevant attributes, must never include a field's existing value, and must never be written to storage. A control's identity may be held in volatile memory beyond the fill only to render that fill's own report, for one fill at a time. | Security | High | **Done by construction** — descriptors carry what a control *is*, never what it holds, and nothing is written to storage. Nothing gates this; see "Guarantees held by construction" above, where the shape assertion that would is named. Amended 2026-08-15 by DD-006: a control's *identity* now outlives the fill, in background memory only, for exactly one fill's report — see "What the fill report retains" above |
| NFR-011 | Reproducible Build         | Building the tagged source in CI must produce a package whose SHA-256 digest matches the published release artefact.            | Maintainability | High     | Done — `scripts/check-reproducible.mjs` runs in CI and digests are published per build |
| NFR-012 | Engine Test Coverage       | The fill engine and generators must reach at least 90% line coverage under unit test.                                           | Maintainability | High     | Done — enforced from 2026-08-15 by globbed thresholds in `vitest.config.ts`: 90% lines and functions across **seven** globs — `lib/page` (96.4% / 98.9%), `lib/generators` (100% / 100%), `lib/persona` (100% / 100%), `lib/rules` (95.9% / 100%), `lib/report` (100% / 100%), `lib/settings.ts` (100% / 100%) and `lib/protocol.ts` (100% / 100%). That the floor grows with the engine is enforced rather than intended: `scripts/check-coverage-scope.mjs` fails the build on any file coverage measures that no threshold glob matches, which is how the last two globs came to exist — at 62% and 22% lines, gated by nothing |
| NFR-013 | Regression Suite           | Each defect listed in `docs/vision.md` §7.2 must have a dedicated failing-then-passing regression test.                         | Maintainability | High     | Done — every defect in §7.2 has a dedicated regression test. D6 was the last, and was untestable until the rule model existed: a randomized-list rule matching a radio group whose values the group does not offer now falls through to the built-in picker instead of indexing an empty array (`tests/rules.test.ts`) |
| NFR-014 | End-to-End Coverage        | An automated browser test must fill the reference page covering every supported control type in Chromium on every change, and in Firefox on every release candidate. Engine unit tests run identically against both targets on every change. | Maintainability | High | Done — both sentences, by two harnesses rather than one. **Chromium**: the packaged extension fills the reference page on every change, plus scopes, cascade, locale, options, settings and profiles. **Firefox**: `engine:firefox` runs the real engine against the same fixture in Gecko on every change — 27 controls across 12 kinds, settling in two passes — and `smoke:firefox` proves the packaged add-on installs and honours `gecko.id` on release candidates. The engine harness needs no extension, which is NFR-015 paying for itself: `runFill` takes a `Document` and a callback, so the code under test is the code the agent runs with the background's half supplied in-page. **What is still not covered, and why it is not effort:** the extension's own trigger in Firefox. A fill starts from a toolbar click, a menu item or a shortcut, and WebDriver input is dispatched into the content area — browser-level shortcut handling is deliberately beyond its reach, measured in both headless and headful Firefox with the correct BiDi modifier codepoints. Chromium has the mirror-image limitation for `activeTab`. So the messaging round trip in Firefox is the remainder, and it is stated rather than implied |
| NFR-015 | Engine Isolation           | The fill engine must contain no reference to the `chrome`/`browser` namespace and must be unit-testable without a browser extension host. | Maintainability | High | Done — the whole page engine, fixpoint loop included, runs under a test DOM with an injected scheduler |
| NFR-016 | Browser Support            | Must run on Chrome 120+, Edge 120+ and Firefox 128+.                                                                            | Portability     | High     | Done — both targets build and load, verified in CI |
| NFR-017 | Single Codebase            | Chromium and Firefox packages must be produced from one source tree with no forked engine or UI code.                           | Portability     | High     | Done — one source tree emits both, with no per-browser branch outside `wxt.config.ts` |
| NFR-018 | Localisation Readiness     | All user-facing strings must be resolved through the i18n message catalog; none hard-coded in components.                       | Portability     | Medium   | Partial — background, manifest and the whole options page resolve through the catalog. Two gaps closed 2026-08-17: the six per-rule source checkboxes were labelled with the `MatchSource` identifiers themselves (`className`, `ariaLabel`), and `validateRule` returned English prose that the page framed with a catalog string, so no translation of a validation failure could be complete — problems now carry a `RuleProblemCode` named for its catalog key, checked by the compiler at the one place it is resolved. What remains English is diagnostic detail carried as substitutions: the position a brace was left open, the group that backtracks, the construct a generator does not support, and the regex engine's own complaint. Each names syntax rather than addressing the user, and localising it means giving `template.ts`, `regex-subset.ts` and `redos.ts` catalogs of their own — worth doing before a second locale ships, not before then |
| NFR-019 | Options UI Accessibility   | The options UI must meet WCAG 2.1 AA, including full keyboard operation of rule reordering.                                     | Usability       | High     | Done — audited 2026-08-17 by `pnpm run a11y`, which drives the real page in Chromium and runs axe-core restricted to the WCAG 2.1 A/AA tags across two editor states (an invalid rule showing its flag and alert, and a scoped rule showing its six checkboxes, both with an undo offer standing) and the report table from a real fill, in both colour schemes; plus reflow at 320 CSS pixels, 200% text at 1280, and the declared palette measured pair by pair. Three violations were found and fixed: `.report-scope-rule` used `opacity: 0.8`, which composited the muted foreground to 4.35:1 against white — under the 4.5:1 small text needs, and invisible to a palette check because the token itself is 7.13:1; and the editor overflowed by 126 px at 320 px, because form controls carry an intrinsic width and grid items do not shrink below it. The judgement-based criteria automation cannot decide are recorded as a checklist at the foot of the audit script. Also done, and what the requirement singles out: reordering is buttons rather than a drag, so it is keyboard-operable by construction, focus follows the button pressed and the new position is announced (BR-012-1, BR-012-2, verified in `e2e-options.mjs`). Every rebuild now places the focus deliberately rather than only the move: expanding returns it to the rule expanded, deleting moves it to the undo offer that replaces it, and undoing puts it on the rule that came back — `replaceChildren` destroys whatever was clicked, so silence there meant `<body>` and a keyboard user starting again from the top of the page (WCAG 2.4.3). Every control is labelled, the disclosure carries `aria-expanded`, validation problems sit in a `role="alert"` and outcomes are named in words rather than by colour alone. What is *not* done is the audit the requirement actually asks for: no WCAG 2.1 AA pass has been run over the page, and contrast ratios and visible-focus styling across the new controls are unmeasured |
| NFR-020 | Failure Legibility         | Every failure path visible to the user must state the cause and the corrective action in one sentence.                          | Usability       | Medium   | Partial — met on the options page as of 2026-08-17: every `ruleProblem*` message names the cause and the correction in one sentence ("the minimum is greater than the maximum. Lower the minimum, or raise the maximum"), and a rejected settings write says what failed and that the rest is unchanged (UC-024 A2/A3). Not met on the badge, which still distinguishes outcomes by colour and a capped fill by tooltip — no sentence, no corrective action |
| NFR-021 | Settings Durability        | No settings write may leave storage in a partially written state; a failed write must leave the previous configuration intact.  | Availability    | High     | Done — a write replaces the whole settings state, never patches it, and the candidate is put through the same parser that reads it back, so a state that survives the write survives the read (BR-024-1, BR-024-7) |
| NFR-022 | Sync Quota Compliance      | Synchronised data must stay within 8 KB per item and 102 KB total, and must degrade to local-only storage rather than fail when exceeded. | Scalability | High | **Done — 2026-08-28.** The layout keeps a real configuration inside 8 KB an item and 102 KB in total; beyond that it degrades to local-only rather than failing, which is exactly UC-029 A1: local storage is the store and the replica is a replica (BR-029-2), so a refused write costs synchronisation and never the configuration. Compliance is not computed from the documented constants — the write is attempted and the store's own refusal is what decides, which is why `isQuotaFailure` distinguishes a quota from a rate limit rather than treating every refusal as a ceiling. Measured capacity is DD-002's: 399 rules, reproduced 2026-08-28. |
| NFR-023 | Sync Write Rate            | Settings writes must be debounced so that no more than 100 sync write operations occur per hour under continuous editing.        | Scalability     | Medium   | **Done — 2026-08-28, and met by arithmetic rather than by hope.** DD-002 measured that the browser counts a write *call* rather than a key, so a sharded save of twenty-odd items costs one. A flush makes at most two calls — a `set`, and a `remove` when a shrunk list dropped a shard — and `SYNC_MIN_INTERVAL_MS` is 72 s, so continuous editing cannot exceed 3600/72 × 2 = 100 operations an hour. The bound is asserted against the constant in `tests/sync.test.ts` because observing it would take an hour and would still have watched one machine; that the gate actually holds a second change back is asserted in the browser. Leading-edge, so a single change after a quiet period goes at once. One consequence is recorded rather than hidden: an MV3 worker may be stopped while a change waits, so a deferred write goes on the next settings change or the next background start, and the screen says the change has not gone rather than when it will. |
| NFR-024 | Rule Scale                 | The options UI and matcher must handle 500 rules with no interaction exceeding 100 ms.                                          | Scalability     | Medium   | Done — measured 2026-08-17 in Chromium by `pnpm run spike:rulescale`, at 500 rules with a third of the matchers regexes and a quarter of the generators regex generators: first render 44 ms median / 56 ms worst, expand a rule 8 ms / 16 ms, move a rule 25 ms / 26 ms, keystroke in an open rule 7 ms / 9 ms. The editor rebuilds the whole list and re-runs `validateRule` per row on every structural change, so the cost is O(rules) per interaction — the measurement is of the constant, and it leaves roughly 4× headroom at the number the requirement names. Nothing about the matcher blocks it: patterns compile once per fill (NFR-025), so rule count costs a test per control, not a construction |
| NFR-025 | Pattern Precompilation     | Rule patterns must be compiled once when settings load and reused; a fill run must construct no `RegExp` per element per rule.  | Performance     | High     | Done — patterns compile once per fill, never per field per pattern (ND-15) |
| NFR-026 | Settings Read Caching      | The background context must serve settings from memory, performing at most one storage read per settings change rather than one per page or frame load. | Performance | Medium | Done — the background serves settings from memory and drops the cache on a storage change |

---

## 3. Constraints

| ID    | Title                        | Constraint                                                                                                                     | Category    | Priority | Status |
|-------|------------------------------|-----------------------------------------------------------------------------------------------------------------------------------|-------------|----------|--------|
| C-001 | Manifest Version             | Must ship Manifest V3; Manifest V2 is no longer accepted by the Chrome Web Store.                                                 | Technical   | High     | **Done** — both targets build Manifest V3 from one source tree, and it is asserted against a *loaded* extension rather than against the file we wrote: `smoke:chrome` reads `manifest_version` back out of `chrome.runtime.getManifest()` in the running service worker, and `smoke:firefox` proves the same manifest installs in Gecko. Both are CI steps on every push. |
| C-002 | Implementation Language      | Must be TypeScript in strict mode; browser extension APIs are JavaScript-only.                                                    | Technical   | High     | **Done** — `tsconfig.json` extends WXT's strict base and adds the five checks it leaves off (`noUncheckedIndexedAccess`, `noImplicitOverride`, `noFallthroughCasesInSwitch`, `exactOptionalPropertyTypes`, `verbatimModuleSyntax`), turned on from the first file rather than after the engine existed. `pnpm run compile` — `tsc --noEmit` over `src`, `tests` and `scripts` — is a CI step on every push, beside `pnpm run lint`. |
| C-003 | Background Model Difference  | Chromium requires `background.service_worker`; Firefox requires `background.scripts`. The manifest must be generated per target.   | Technical   | High     | **Done** — one source tree emits both, with `wxt.config.ts` the only per-target branch anywhere (NFR-013's own claim). The built manifests carry `background.service_worker` for Chromium and `background.scripts` for Firefox, and each is *loaded* in CI rather than inspected: `smoke:chrome` attaches to the worker, `smoke:firefox` installs the add-on and names this constraint in its own failure text. |
| C-004 | Firefox Identity             | The Firefox package must declare `browser_specific_settings.gecko.id`.                                                            | Technical   | High     | **Done** — `browser_specific_settings.gecko.id` is `fieldfiller@dividbzero`, and `smoke:firefox` asserts Firefox assigned *that* id rather than a generated one. The check is worth more than a manifest read: an id the browser declines to honour is how an AMO listing silently becomes a second extension on the next release. |
| C-005 | Sync Storage Limits          | `storage.sync` enforces 8,192 bytes per item, 102,400 bytes total, and 1,800 writes per hour.                                     | Technical   | High     | **Done — verified in the browser rather than quoted from the docs** (`pnpm run spike:syncquota`, Chromium, 2026-08-19 and re-run 2026-08-28). Every figure in this row is read off `chrome.storage.sync` in a running browser: 8,192 bytes per item, 102,400 total, 1,800 writes an hour — and 512 items and 120 writes a minute, which this row did not name and which both bind. A control probe writes an oversized item first and fails the run if it is *accepted*, because a browser not enforcing the quota would let every measurement through and report a comfortable pass meaning nothing. Two facts the constraint's wording hid: the browser counts **UTF-8 bytes, not UTF-16 code units**, so a de-CH configuration is quietly larger than an en-US one of the same shape; and a write operation is counted per **call**, not per key, so a sharded save of twenty-odd keys costs one. Complying with it is DD-002's layout, built as UC-029 on 2026-08-28 and carried by NFR-022 and NFR-023. |
| C-006 | Closed Shadow Roots          | Controls inside closed shadow roots are unreachable by any extension API and are permanently out of scope.                        | Technical   | High     | **Done by construction** — no extension API reaches a closed root, so this is a boundary rather than a decision. What is testable is that the boundary is drawn where it says: FR-008's harness fills a control inside an *open* root and asserts a closed one is left alone, so "out of scope" is a demonstrated behaviour and not an untested assumption about where the walk stops. |
| C-007 | Cross-Origin Frames          | Filling inside a cross-origin iframe requires injection into that frame; it cannot be reached from the parent document.           | Technical   | High     | **Done** — the manifest declares `all_frames` with `match_about_blank`, which is DD-001's persistent injection paying for exactly this. FR-011 fills same-origin and cross-origin frames from one persona end-to-end, so the constraint is discharged by the frames actually filling rather than by the declaration being present. |
| C-008 | No Backend or Phoning-Home Dependency | No server-side component or account system may be introduced (per PD-001), and no runtime dependency shipped in the bundle may issue network requests, persist state outside the extension's own storage, or execute remote code. Build-time and development tooling are unconstrained by this. | Technical | High | **Done, and gated twice.** `check-network.mjs` fails the build on any network or remote-code capability in any shipped file of either target, and `check-imports.mjs` walks the page agent's import graph against an allowlist — `package.json` declares no `dependencies` field at all, so there is no runtime dependency to constrain. Both run in `gate:all` on every push. **Amended 2026-08-28 by UC-029:** synchronised settings leave the device. They stay inside "the extension's own storage" as this constraint words it — `storage.sync` is the browser's, reached through the same `storage` permission, with no server of ours and no account of ours (BR-029-1) — but the privacy *claim* changes shape and the constraint is not where that gets to go unsaid: see C-011 and BR-029-1 for the words that have to change with it. |
| C-009 | Upstream Attribution         | The upstream MIT copyright notice and permission text must be retained and shipped with the extension.                            | Regulatory  | High     | **Partial — retained, not yet shipped.** `NOTICE` carries the upstream MIT notice in full, with the reasoning for reproducing it at all (a reimplementation this closely informed by its predecessor is not a place to be clever about where study ends), and it travels in the sources archive `wxt zip` builds. It is not in the extension package, and the surface that would show it to a user inside the product is FR-062 — `Open`, Phase 7. The constraint says *shipped with the extension*, and that half is owed. |
| C-010 | Trademark Separation         | The name, logo, icons, store imagery and listing copy must share nothing with "Fake Filler".                                       | Regulatory  | High     | **Partial — nothing shipped shares anything, and the listing is not finished.** The name, the mark, the icons and both promo tiles are original, and their provenance is the committed generator that draws them (`make-icons.mjs`, `make-promo.mjs`) rather than a claim about where they came from. `NOTICE` §3 disclaims the mark explicitly, and `store_listing.md` carries the rule that overrides every copy decision in it — the listing never names the reference, not even as a comparison or a migration keyword. What is outstanding is the imagery that does not exist yet: the store screenshots, without which no listing goes live. A constraint over copy and images cannot be certified over copy and images nobody has written. |
| C-011 | Store Privacy Disclosure     | Store listings must declare data handling; the declaration must state that no user data is collected.                             | Regulatory  | High     | **Partial — written, unpublished, and about to need a sentence it does not have.** `store_listing.md` carries both questionnaires answered — every data type `No`, all three Chrome certifications affirmed, and Firefox's `data_collection_permissions: { required: ["none"] }` already in the shipped manifest. `PRIVACY.md` exists in the repository. Neither store accepts a listing without a *reachable* privacy-policy URL, and it is not published. **And UC-029 changes what it must say** (BR-029-1): "stored on the device" becomes "stored on the device and, where you have turned it on, in your browser's own synchronisation". That sentence is owed to this row before the feature ships, not after somebody asks. |
| C-012 | Store Review Policy          | The package must satisfy Chrome Web Store and AMO review policy, including the single-purpose rule and no obfuscated code.        | Regulatory  | High     | **Partial — the halves that can be evidenced are, and the verdict is the reviewer's.** No remote code, gated (`check-network.mjs`); no obfuscation — the bundle is minified, which both stores permit, and the readable sources travel in the archive `wxt zip` builds, which is what AMO asks for when a submission is minified; single purpose, which the permission gate keeps honest by refusing anything outside the five NFR-008 names. What cannot be scored from here is the review itself: no submission has been made, so this row is `Partial` until one comes back rather than `Done` on the strength of our own reading of the policy. |
| C-013 | Solo Maintainer              | Scope must fit one developer; anything requiring ongoing operational duty (a backend, support inbox, billing) is out.              | Business    | High     | **Done by construction, and it is the constraint that decided DD-002.** There is no backend, no account system, no billing and no support infrastructure to run, and the gates that hold C-008 hold this with it. The clearest evidence is a decision taken *because* of it: synchronisation could have been an account of ours and is instead the browser's own sync, sharded — the user's existing trust relationship rather than a new operational duty (BR-029-1). |
| C-014 | Public Development           | All source that produces a released artefact must be public at the tagged commit (per G4).                                        | Operational | High     | **Partial — the machinery runs, the publication is Phase 7.** `check-reproducible.mjs` and `digest.mjs` are CI steps on every push, so a released artefact's digest is reproducible from the tagged source rather than asserted to be, and `wxt zip` emits a sources archive beside each package. What is not built is the half an auditor uses: publishing the source tag and the artefact digest where they can be reached and compared, which is UC-032 — `Open`, Phase 7, and the requirement that carries it is FR-063. |

---

## 4. Known Conflicts

| Conflict | Detail | Resolution |
|---|---|---|
| ~~NFR-008 vs FR-003~~ | Minimal permissions favoured on-demand injection, but "fill this input" needs a listener present *before* the right-click that identifies the target element. | **Resolved 2026-08-12 (DD-001):** persistent injection chosen. Chrome exposes no element identifier on context-menu clicks, so on-demand cannot support FR-003 reliably. NFR-008 rewritten; the minimal-permission claim is withdrawn rather than fudged. |
| ~~FR-037 vs FR-074~~ | Regular-expression domain patterns cannot be translated into the host match patterns the browser needs in order to *not* inject. | **Resolved 2026-08-15 (BR-008-4):** the two tiers were the expected answer and were not taken. Non-injection needs runtime content-script registration in place of the manifest's static declaration, and that registration is state that fails silently in the direction that matters — an agent absent where the user wanted a fill, or present where they did not. Both tiers are therefore injected-then-inert, which makes the pattern kinds *equivalent* rather than something the UI must keep apart: the conflict was between two ways of not injecting, and neither is used. FR-074 was rewritten to claim only what is true. The structural version stays available without rework. |
| **FR-058 vs DD-002** | Cross-device consistency wants a device's push to carry only what that device changed; the sharded layout has no way to tell "I changed this key" from "another device changed it", so a push computed against a stale baseline can revert a concurrent change by more than the eight rules BR-029-3 states. | **Accepted 2026-08-28, not dissolved.** The echo — every adoption used to provoke one — is closed at the source. The remainder needs a generation per shard and a compare-and-set, which changes the stored layout and would silently empty rule lists on any device still running today's build (UC-029 A6, FR-073's ladder). Deferred with its trigger written down rather than left as a discrepancy between the screen and the store. The standing sentence keeps its number: it describes what the design does, the exception is rare and documented in UC-029 A2, and hedging a permanent one-line warning costs more clarity than the case is worth. |
| ~~FR-058 vs C-005/NFR-022~~ | Sync must carry an unlimited rule set (FR-044 as written until 2026-08-28) through an 8 KB-per-item quota. **Built out on 2026-08-28**: FR-044 no longer claims an unlimited set, and UC-029 says where the store stops before the user reaches it. | **Resolved 2026-08-22 (DD-002), by measurement and then by accepting a bound rather than by dissolving it** (`pnpm run spike:syncquota`). Sharding is the only layout that carries a real configuration, and it moves the conflict from the per-item quota to the total one rather than removing it: **399 rules** at the chosen eight-rule shard, ~230 B a rule. FR-044's *unlimited* set cannot be synchronised in full at any layout, so the requirement it is in tension with is answered by FR-059 — UC-029 says sync has stopped rather than truncating silently. FR-044 itself is unchanged: it caps nothing locally, and sync was never the store. |
| ~~FR-019 vs NFR-003~~ | Richer generated data argued for a large corpus; the page-agent size budget argued against. | **Resolved 2026-08-12 (DD-003):** generation moved to the background. The corpus never enters the page agent, so the two requirements no longer compete. New cost is a round trip and a cold start, bounded by NFR-027..029. |
| **FR-081 vs NFR-003** | Detecting and driving custom comboboxes is agent-side code, and detection also widens the walk's candidate selector on every page — spending latency budget everywhere to serve a minority of pages. | **Resolved 2026-08-15 by measurement, not by tiebreak** (`scripts/spike-combobox.mjs`). On a 500-control application page with no combobox, the widened selector adds **zero** candidates and 0.05 ms — an attribute selector matches only elements carrying the attribute, so nothing downstream sees any difference. The ladder cost 3.8 KB, taking the agent to 18.5 KB of 40 KB. The conflict was between a real cost and an imagined one. What does cost is *driving* each control, which NFR-036 bounds instead. |
| **FR-077 vs NFR-010** | Deciding whether our write survived means reading a control's current contents, which is page-originated data. | Resolved by scope, not exception: the comparison happens entirely inside the page agent and within the operation that made the write. No value read back is placed in a descriptor, a report or a message, so NFR-010 and NFR-030 hold unchanged. Recorded here because the requirement reads as forbidding it at a glance. |
